--- a/main.py	2026-08-04 23:51:09.675309571 +0000
+++ b/main.py	2026-08-04 23:49:03.437975500 +0000
@@ -5,13 +5,13 @@
 
 미국 정규장 마감(16:00 ET) 직후에 최대한 바로 실행되도록 뉴욕시간
 기준으로 시간대를 직접 판단함(서머타임 자동 반영, KST 환산 필요 없음).
-마감 정각~+15분 사이에만 동작하고, 그 안에서도 하루 딱 1번만 실행되도록
-날짜 기준 중복방지 처리함(eod_sent.json) - 워크플로우가 5분 간격으로
-여러 번 깨어나도 실제로는 그 날의 첫 실행 때만 동작함.
-
-마감 직후 15분 이내로 당긴 이유: 야후파이낸스 등 데이터 제공처가 정규장
-종가를 확정하는 데 몇 분 정도 걸릴 수 있어서, 최소한의 안전 여유만 두고
-최대한 빠르게 실행함 (애프터마켓 시간대에 걸쳐 신호를 놓치지 않도록).
+마감 정각~+300분(5시간) 사이에만 동작하고, 그 안에서도 하루 딱 1번만
+실행되도록 날짜 기준 중복방지 처리함(eod_sent.json) - 워크플로우가 5분
+간격으로 여러 번 깨어나도 실제로는 그 날의 첫 실행 때만 동작함.
+
+윈도우를 5시간이나 넉넉하게 잡은 이유: GitHub Actions 예약 실행이 혼잡할 때
+몇 시간씩 늦게 트리거되는 경우가 실제로 있어서(2026-08-04에 마감+1시간41분
+지연 사례 확인됨), 너무 빡빡하게 잡으면 그날 리포트를 통째로 놓칠 수 있음.
 
 사용법:
   pip install -r requirements.txt
@@ -40,13 +40,13 @@
 
 NY_TZ = ZoneInfo("America/New_York")
 
-# 마감(16:00 ET) 정각부터 +120분까지 "마감 직후"로 인정.
+# 마감(16:00 ET) 정각부터 +300분(5시간)까지 "마감 직후"로 인정.
 # (GitHub Actions가 혼잡할 때 예약 실행이 몇 시간씩 늦게 트리거되는 경우가
-#  실제로 있어서, 너무 빡빡하게 잡으면 그날 리포트를 통째로 놓칠 수 있음.
-#  중복 전송은 eod_sent.json(하루 1번 마커)이 따로 막아주므로, 이 윈도우를
-#  넉넉하게 잡아도 스팸 걱정은 없음.)
+#  실제로 있어서 - 8/4에 실제로 마감+1시간41분 지연된 사례가 확인됨 -
+#  기존 120분으로는 부족했음. 중복 전송은 eod_sent.json(하루 1번 마커)이
+#  따로 막아주므로, 이 윈도우를 넉넉하게 잡아도 스팸 걱정은 없음.)
 CLOSE_TIME = (16, 0)
-WINDOW_MINUTES_AFTER_CLOSE = 120
+WINDOW_MINUTES_AFTER_CLOSE = 300
 
 SENT_MARKER_FILE = Path(__file__).parent / "eod_sent.json"
 
--- a/premarket_digest.py	2026-08-04 23:51:09.675560289 +0000
+++ b/premarket_digest.py	2026-08-04 23:49:45.421305807 +0000
@@ -34,10 +34,13 @@
 
 NY_TZ = ZoneInfo("America/New_York")
 
-# 장 시작(09:30 ET) 20분 전 = 09:10 ET. cron 오차/재실행 감안해서
-# 09:00~09:20 사이 아무 때나 실행되면 "그날의 리마인더"로 인정함.
+# 장 시작(09:30 ET) 20분 전 = 09:10 ET가 목표 시각이지만, GitHub Actions
+# 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로 있어서(EOD에서
+# 2026-08-04에 마감+1시간41분 지연 사례 확인됨) 09:00~12:00 사이 아무
+# 때나 실행되면 "그날의 리마인더"로 넉넉하게 인정함. 중복 전송은
+# premarket_sent.json(하루 1번 마커)이 막아주므로 넓게 잡아도 스팸 없음.
 WINDOW_START = (9, 0)
-WINDOW_END = (9, 20)
+WINDOW_END = (12, 0)
 
 SENT_MARKER_FILE = Path(__file__).parent / "premarket_sent.json"
 
--- a/kr_open_digest.py	2026-08-04 23:51:09.675139325 +0000
+++ b/kr_open_digest.py	2026-08-04 23:50:08.459465365 +0000
@@ -38,10 +38,13 @@
 
 KST_TZ = ZoneInfo("Asia/Seoul")
 
-# 한국 장 시작(09:00 KST) 20분 전 = 08:40 KST. cron 오차 감안해서
-# 08:30~08:50 사이 아무 때나 실행되면 "그날의 리마인더"로 인정함.
+# 한국 장 시작(09:00 KST) 20분 전 = 08:40 KST가 목표 시각이지만, GitHub
+# Actions 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로 있어서(EOD에서
+# 2026-08-04에 마감+1시간41분 지연 사례 확인됨) 08:30~11:00 사이 아무
+# 때나 실행되면 "그날의 리마인더"로 넉넉하게 인정함. 중복 전송은
+# kr_open_sent.json(하루 1번 마커)이 막아주므로 넓게 잡아도 스팸 없음.
 WINDOW_START = (8, 30)
-WINDOW_END = (8, 50)
+WINDOW_END = (11, 0)
 
 SENT_MARKER_FILE = Path(__file__).parent / "kr_open_sent.json"
 
--- a/.github/workflows/eod_report.yml	2026-08-04 23:51:09.666817082 +0000
+++ b/.github/workflows/eod_report.yml	2026-08-04 23:49:24.680002356 +0000
@@ -2,19 +2,23 @@
 
 # 미국 정규장 마감(16:00 ET) 이후 최대한 놓치지 않고 실행되도록, 넉넉한
 # 범위를 자주(5분 간격, 혼잡시간 회피) 깨워서 체크함. 실제로 "마감 이후
-# 120분 이내"인지는 main.py 내부의 zoneinfo 체크가 정확하게 걸러주고,
+# 300분(5시간) 이내"인지는 main.py 내부의 zoneinfo 체크가 정확하게 걸러주고,
 # 하루 1번만 실행되도록 eod_sent.json으로 중복도 방지함 (그래서 아래
 # 범위 안에서 여러 번 깨어나도 실제로는 그날의 첫 실행 때만 동작함).
 #
-# 120분(2시간)이나 여유를 두는 이유: GitHub Actions는 혼잡한 시간대에
-# 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로 있어서, 너무
-# 빡빡하게 잡으면 그날 리포트를 통째로 놓칠 수 있음.
+# 300분(5시간)이나 여유를 두는 이유: GitHub Actions는 혼잡한 시간대에
+# 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로 있어서(2026-08-04에
+# 마감+1시간41분 지연으로 그날 놓친 사례 확인됨), 너무 빡빡하게 잡으면
+# 그날 리포트를 통째로 놓칠 수 있음.
 #
 # EDT(서머타임) 기준 마감 = 20:00 UTC, EST 기준 마감 = 21:00 UTC
-# -> 20:00~23:00 UTC 범위를 커버해서 두 계절 다 + 지연 여유까지 포함.
+# -> 20:00 UTC부터 자정 넘어 다음날 01:58 UTC까지 커버해서 두 계절 다 +
+# 5시간 지연 여유까지 포함 (자정을 넘어가서 요일 두 개로 나눠 등록 -
+# intraday_monitor.yml과 같은 방식).
 on:
   schedule:
     - cron: '3,8,13,18,23,28,33,38,43,48,53,58 20-23 * * 1-5'
+    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 0-1 * * 2-6'
   workflow_dispatch:
     inputs:
       test:
--- a/.github/workflows/premarket_digest.yml	2026-08-04 23:51:09.667101783 +0000
+++ b/.github/workflows/premarket_digest.yml	2026-08-04 23:49:59.655042131 +0000
@@ -1,15 +1,17 @@
 name: Premarket Digest
 
-# 미국 정규장 시작(09:30 ET) 20분 전 = 09:10 ET 부근에 실행.
+# 미국 정규장 시작(09:30 ET) 20분 전 = 09:10 ET 부근이 목표 시각.
 # EDT(서머타임) 기준 09:00~09:20 ET = 13:00~13:20 UTC
 # EST 기준 09:00~09:20 ET = 14:00~14:20 UTC
-# -> 두 경우 다 커버하도록 13:00~14:20 UTC 범위에서 10분 간격으로 깨움.
+# -> GitHub Actions 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로
+# 있어서(EOD에서 2026-08-04에 마감+1시간41분 지연 사례 확인됨) 13~17 UTC
+# 로 넉넉하게 잡음(=ET 09:00~13:00 부근까지 커버, 두 계절 다 포함).
 # (실제로 "지금이 리마인더 시간대인지"는 premarket_digest.py 내부의
 #  zoneinfo 체크가 다시 한 번 정확하게 걸러주고, 하루 1번만 보내지도록
 #  premarket_sent.json으로 중복도 방지함)
 on:
   schedule:
-    - cron: '4,14,24,34,44,54 13-14 * * 1-5'
+    - cron: '4,14,24,34,44,54 13-17 * * 1-5'
   workflow_dispatch:
     inputs:
       test:
--- a/.github/workflows/kr_open_digest.yml	2026-08-04 23:51:09.667008221 +0000
+++ b/.github/workflows/kr_open_digest.yml	2026-08-04 23:50:21.914343963 +0000
@@ -1,15 +1,19 @@
 name: KR Market Open Digest
 
-# 한국 장 시작(09:00 KST) 20분 전 = 08:40 KST 부근에 실행.
+# 한국 장 시작(09:00 KST) 20분 전 = 08:40 KST 부근이 목표 시각.
 # 한국은 서머타임이 없어서 UTC로 고정 계산 가능: 08:40 KST = 23:40 UTC(전날).
 # KST 월~금(거래일)에 맞추려면, UTC 기준으로는 일~목(전날 밤)이 되어야 함
 # (cron day-of-week: 0=일,1=월,...,4=목).
-# 여유 있게 23:00~23:55 UTC 사이 5분 간격으로 깨우고, 실제로 08:30~08:50
-# KST인지는 kr_open_digest.py 내부에서 정확히 판단함. 하루 1번만 실행되도록
-# kr_open_sent.json으로 중복도 방지함.
+# GitHub Actions 예약 실행이 몇 시간씩 늦게 트리거되는 경우가 실제로
+# 있어서(EOD에서 2026-08-04에 마감+1시간41분 지연 사례 확인됨) 23:00 UTC
+# (전날 밤)부터 자정 넘어 다음날 02:58 UTC(=KST 11:58)까지 넉넉하게 깨움
+# (자정을 넘어가서 요일 두 개로 나눠 등록 - eod_report.yml과 같은 방식).
+# 실제로 08:30~11:00 KST인지는 kr_open_digest.py 내부에서 정확히 판단함.
+# 하루 1번만 실행되도록 kr_open_sent.json으로 중복도 방지함.
 on:
   schedule:
     - cron: '3,8,13,18,23,28,33,38,43,48,53,58 23 * * 0-4'
+    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 0-2 * * 1-5'
   workflow_dispatch:
     inputs:
       test:
